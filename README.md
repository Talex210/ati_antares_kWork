# 📦 ATI.SU → Telegram Integration

Автоматическая система публикации грузов из ATI.SU в Telegram-группу с веб-панелью управления.

## 🎯 Описание проекта

Приложение автоматически мониторит API ATI.SU, фильтрует грузы по белому списку логистов и добавляет их в очередь на публикацию. Через веб-интерфейс администратор может управлять логистами, просматривать грузы и публиковать их в нужные топики Telegram-группы.

## ✨ Основные возможности

### 🤖 Telegram Bot
- Автоматический мониторинг API ATI.SU каждые 5 минут
- Двусторонняя синхронизация: грузы, удаленные из ATI.SU, автоматически удаляются из очереди.
- Фильтрация грузов по белому списку логистов
- Публикация грузов в различные топики Telegram-группы
- Форматированные сообщения с полной информацией о грузе

### 🌐 Веб-панель управления
- **Управление логистами:**
  - Добавление логистов по номеру телефона и Telegram-нику
  - Автоматический поиск контактов в ATI.SU
  - Удаление логистов из белого списка
  - Обновление контактной информации

- **Управление грузами:**
  - Просмотр очереди грузов на публикацию
  - Фильтрация грузов по логистам (горизонтальные чекбоксы)
  - Выбор топика для публикации
  - Публикация или отклонение грузов (включая массовые операции)
  - Ручной запуск пересканирования грузов

- **Архив отклоненных грузов:**
  - Просмотр списка отклоненных грузов
  - Возможность восстановить груз обратно в очередь
  - Окончательное удаление груза из архива

- **Управление топиками:**
  - Создание, редактирование и удаление топиков для публикации в Telegram.
  - Назначение грузов на конкретные топики при публикации.

- **Безопасность:**
  - Аутентификация по паролю
  - Защита всех API endpoints

## 🛠️ Технологии

### Backend
- **Node.js** 20+ с TypeScript
- **Express.js** - веб-сервер и REST API
- **node-telegram-bot-api** - интеграция с Telegram
- **SQLite** - локальная база данных
- **Axios** - HTTP-клиент для ATI API

### Frontend
- Vanilla JavaScript (без фреймворков)
- HTML5 + CSS3
- Адаптивный дизайн

### DevOps
- **PM2** - менеджер процессов
- **Git** - контроль версий
- **ESLint + Prettier** - качество кода

## 📋 Требования

- Node.js 20.x или выше
- npm 10.x или выше
- Telegram Bot Token (от @BotFather)
- ATI.SU API Token
- VPS сервер (минимум 1 ГБ RAM, 1 CPU)

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/Talex210/ati_antares_kWork.git
cd ati_antares_kWork
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Токен Telegram-бота от @BotFather
TELEGRAM_BOT_TOKEN="your_bot_token_here"

# ID Telegram-группы (со знаком минус)
TELEGRAM_CHAT_ID="-1001234567890"

# Пароль для входа в админ-панель
ADMIN_PASSWORD=your_secure_password

# Токен API ATI.SU
ATI_API_TOKEN=your_ati_token_here

# Порт для веб-сервера (опционально, по умолчанию 3000)
PORT=3000
```

### 4. Сборка проекта

```bash
npm run build
```

### 5. Запуск приложения

**Для разработки:**
```bash
npm run start:dev
```

**Для продакшена:**
```bash
npm start
```

**С PM2 (рекомендуется для сервера):**
```bash
pm2 start dist/index.js --name ati-bot
pm2 save
pm2 startup
```

### 6. Доступ к админ-панели

Откройте в браузере: `http://localhost:3000`

Введите пароль из `.env` файла.

## 📁 Структура проекта

```
ati_antares_kWork/
├── src/
│   ├── api/
│   │   └── router.ts          # REST API endpoints
│   ├── core/
│   │   ├── format.ts          # Форматирование сообщений
│   │   └── types.ts           # TypeScript типы
│   ├── api.ts                 # Сервис для ATI API
│   ├── ati_api.ts             # Клиент ATI API
│   ├── bot.ts                 # Telegram Bot логика
│   ├── database.ts            # Работа с SQLite
│   ├── synchronizer.ts        # Логика полной синхронизации данных
│   └── index.ts               # Точка входа
├── public/
│   ├── index.html             # Веб-интерфейс
│   ├── script.js              # Frontend логика
│   └── style.css              # Стили
├── scripts/
│   ├── add-logisticians.js    # Скрипт добавления логистов
│   └── clear-published.js     # Очистка опубликованных
├── .env                       # Переменные окружения (не в git)
├── database.db                # SQLite база (создается автоматически)
├── package.json
├── tsconfig.json
├── DEPLOY.md                  # Инструкция по развертыванию
└── README.md                  # Этот файл
```

## 🗄️ База данных

Приложение использует SQLite с пятью основными таблицами:

- **whitelisted_logisticians** - белый список логистов
- **pending_loads** - грузы в очереди на публикацию
- **published_loads** - история опубликованных грузов
- **rejected_loads** - архив отклоненных грузов
- **topics** - справочник топиков для публикации

База создается автоматически при первом запуске.

## 🔧 Доступные команды

```bash
# Разработка
npm run start:dev          # Запуск с hot-reload
npm run build              # Сборка TypeScript → JavaScript
npm start                  # Запуск продакшен версии

# Качество кода
npm run lint               # Проверка кода ESLint
npm run lint:fix           # Автоисправление ESLint
npm run format             # Форматирование Prettier

# Утилиты
npm run sync               # Принудительная полная синхронизация данных с ATI.SU
npm run clear:published    # Очистка опубликованных грузов
npm run add:logisticians   # Добавление логистов из скрипта
```

## 🌐 API Endpoints

### Аутентификация
Все endpoints требуют заголовок:
```
Authorization: Bearer YOUR_ADMIN_PASSWORD
```

### Логисты
- `GET /api/logisticians` - Получить список логистов
- `POST /api/logisticians` - Добавить логиста по ATI ID (для скриптов)
- `POST /api/logisticians/add-by-phone` - Добавить логиста по телефону
- `DELETE /api/logisticians/:id` - Удалить логиста
- `POST /api/logisticians/update-contacts` - Обновить контакты

### Грузы
- `GET /api/pending-loads` - Получить грузы в очереди
- `POST /api/publish` - Опубликовать груз
- `POST /api/publish-loads` - Опубликовать несколько грузов
- `POST /api/reject-load` - Отклонить груз
- `POST /api/reject-loads` - Отклонить несколько грузов
- `POST /api/rescan-loads` - Принудительное пересканирование

### Отклоненные грузы
- `GET /api/rejected-loads` - Получить отклоненные грузы
- `POST /api/restore-load` - Восстановить груз
- `DELETE /api/rejected-loads/:loadId` - Удалить навсегда

### Топики
- `GET /api/topics` - Получить список топиков
- `POST /api/topics` - Создать новый топик
- `PUT /api/topics/:id` - Обновить топик
- `DELETE /api/topics/:id` - Удалить топик

### Утилиты
- `GET /api/contacts` - Получить контакты из ATI
- `POST /api/cities` - Получить информацию о городах
- `POST /api/delete-message` - Удалить сообщение в Telegram

## 🚀 Развертывание на сервере

Подробная инструкция по развертыванию находится в файле [DEPLOY.md](DEPLOY.md).

### Краткая версия:

1. Подключитесь к серверу по SSH
2. Установите Node.js 20+, Git, PM2
3. Клонируйте репозиторий
4. Настройте `.env` файл
5. Установите зависимости: `npm install`
6. Соберите проект: `npm run build`
7. Запустите с PM2: `pm2 start dist/index.js --name ati-bot`
8. Настройте автозапуск: `pm2 save && pm2 startup`

## 🔄 Обновление на сервере

```bash
cd /root/ati_antares_kWork
git pull
npm install
npm run build
pm2 restart ati-bot
```

## 📊 Мониторинг

### Просмотр логов
```bash
pm2 logs ati-bot --lines 50
```

### Статус приложения
```bash
pm2 status
```

### Использование ресурсов
```bash
pm2 monit
```

## 🐛 Решение проблем

### Приложение не запускается
1. Проверьте логи: `pm2 logs ati-bot`
2. Убедитесь, что все переменные в `.env` заполнены
3. Проверьте версию Node.js: `node -v` (должна быть 20+)

### Грузы не появляются
1. Проверьте, что логисты добавлены в белый список
2. Убедитесь, что ATI API Token валиден
3. Проверьте логи на ошибки API

### Бот не отвечает
1. Проверьте TELEGRAM_BOT_TOKEN
2. Убедитесь, что бот добавлен в группу
3. Проверьте TELEGRAM_CHAT_ID (должен начинаться с минуса)

## 🔐 Безопасность

- Никогда не коммитьте `.env` файл в Git
- Используйте сильный пароль для админ-панели
- Регулярно обновляйте зависимости: `npm audit fix`
- Ограничьте доступ к серверу через firewall
- Используйте HTTPS для продакшена (через Nginx + Let's Encrypt)

## 📝 Roadmap

Планы развития проекта находятся в файле [roadmap.md](roadmap.md).

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте ветку: `git checkout -b feature/amazing-feature`
3. Commit изменений: `git commit -m 'Add amazing feature'`
4. Push в ветку: `git push origin feature/amazing-feature`
5. Откройте Pull Request

## 📄 Лицензия

ISC License

## 👨‍💻 Автор

GitHub: [@Talex210](https://github.com/Talex210)

## 📞 Поддержка

При возникновении вопросов или проблем:
- Создайте [Issue](https://github.com/Talex210/ati_antares_kWork/issues)
- Проверьте [DEPLOY.md](DEPLOY.md) для инструкций по развертыванию

---

**Дата создания:** Ноябрь 2025  
**Версия:** 1.0.0
