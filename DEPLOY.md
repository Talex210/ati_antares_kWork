# 🚀 Инструкция по развертыванию и обновлению на сервере

## 📋 Данные сервера

- **IP-адрес:** 37.143.13.205
- **Hostname:** p913451.kvmvps
- **Логин:** root
- **Пароль:** sHW6deVNc7
- **Путь к проекту:** /root/ati_antares_kWork

---

## 🔄 Обновление приложения после изменений в GitHub

### Шаг 1: Подключитесь к серверу

1. Откройте **PuTTY**
2. В поле **Host Name** введите: `37.143.13.205`
3. Нажмите **Open**
4. Введите логин: `root`
5. Введите пароль: `sHW6deVNc7`

### Шаг 2: Выполните команды обновления

Скопируйте и выполните эти команды по очереди:

```bash
cd /root/ati_antares_kWork
```

```bash
git pull
```

```bash
npm install
```

```bash
npm run build
```

```bash
pm2 restart ati-bot
```

```bash
pm2 logs ati-bot --lines 30
```

### ✅ Готово!

Приложение обновлено и перезапущено. Последняя команда покажет логи - убедитесь, что нет ошибок.

---

## 🛠️ Полезные команды PM2

### Проверить статус приложения
```bash
pm2 status
```

### Посмотреть логи (последние 50 строк)
```bash
pm2 logs ati-bot --lines 50
```

### Посмотреть логи в реальном времени
```bash
pm2 logs ati-bot
```
*Для выхода нажмите `Ctrl + C`*

### Остановить приложение
```bash
pm2 stop ati-bot
```

### Запустить приложение
```bash
pm2 start ati-bot
```

### Перезапустить приложение
```bash
pm2 restart ati-bot
```

### Посмотреть использование ресурсов
```bash
pm2 monit
```
*Для выхода нажмите `Ctrl + C`*

---

## 🔧 Редактирование .env файла на сервере

Если нужно изменить переменные окружения:

```bash
cd /root/ati_antares_kWork
nano .env
```

После редактирования:
1. Нажмите `Ctrl + X`
2. Нажмите `Y`
3. Нажмите `Enter`
4. Перезапустите приложение: `pm2 restart ati-bot`

---

## 🌐 Доступ к приложению

- **Админ-панель:** http://37.143.13.205:3000
- **Пароль админки:** ati_5543

---

## 🆘 Решение проблем

### Приложение не запускается

```bash
cd /root/ati_antares_kWork
pm2 logs ati-bot --lines 50
```

Посмотрите на ошибки в логах.

### Проверить, что Node.js установлен

```bash
node -v
npm -v
```

### Полная переустановка приложения

```bash
cd /root/ati_antares_kWork
pm2 delete ati-bot
rm -rf node_modules dist
npm install
npm run build
pm2 start dist/index.js --name ati-bot
pm2 save
```

---

## 📞 Контакты

При возникновении проблем обращайтесь к разработчику.

**Дата создания инструкции:** 14 ноября 2025
